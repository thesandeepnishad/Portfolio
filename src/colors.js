/*
 * ============================================
 *  CENTRALIZED COLOR PALETTE
 * ============================================
 *  Change colors HERE and they update everywhere.
 *  After editing, save the file — HMR will reload.
 *
 *  Roles:
 *    bg        — Main page background
 *    card      — Card / panel background
 *    text      — Body text color
 *    heading   — Hero headings, gradient titles (bright)
 *    accent    — Primary accent (buttons, links)
 *    highlight — Secondary lighter accent
 *    glow1     — Sphere / glow color 1 (cyan-ish)
 *    glow2     — Sphere / glow color 2 (deeper)
 *    spark     — Sparkle / shine color
 *    hover     — Button / interactive hover color
 * ============================================
 */

const palette = {
  bg:        "#241571",   // Midnight — page background
  card:      "#2438BD",   // Persian  — card backgrounds
  text:      "#99CCFF",   // Sky      — body text
  heading:   "#ADFFFF",   // Celeste  — headings, gradient titles
  accent:    "#0015FF",   // Blue     — buttons, primary accent
  highlight: "#70B8FF",   // Argentina — secondary accent, borders
  glow1:     "#0AFFFF",   // Aqua     — sphere glow 1
  glow2:     "#007BA7",   // Cerulean — sphere glow 1 edge
  spark:     "#52E8FF",   // Electric — sparkles, shine
  hover:     "#007FFF",   // Azure    — hover states
};

export default palette;
